<%= form_for(@kamoku) do |f| %>
  <% if @kamoku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kamoku.errors.count, "error") %> prohibited this kamoku from being saved:</h2>

      <ul>
      <% @kamoku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
