<p id="notice"><%= notice %></p>

<h1>科目一覧</h1>

<table>
  <thead>
    <tr>
      <th>科目名</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @kamokus.each do |kamoku| %>
      <tr>
        <td><%= kamoku.name %></td>
        <td><%= link_to '詳細', kamoku %></td>
        <td><%= link_to '編集', edit_kamoku_path(kamoku) %></td>
        <td><%= link_to '削除', kamoku, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '科目登録', new_kamoku_path %>
